name: Deploy Frontend to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Clean install
        working-directory: frontend
        run: |
          rm -rf node_modules
          npm install

      - name: List components (debug)
        run: ls frontend/src/components/

      - name: Build
        working-directory: frontend
        env:
          VITE_API_BASE: https://luminous-carbon-atlas.onrender.com
        run: npm run build

      - name: Upload to GitHub Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: frontend/dist

      - name: Deploy
        uses: actions/deploy-pages@v4
